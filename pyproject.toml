[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "42 A_maze_ing Maze Generator."
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "nlicot-d", email="nlicot-d@student.42madrid.com" },
    { name = "pcordero", email="pcordero@student.42madrid.com" },
]
readme = "README.md"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["mazegen*"]